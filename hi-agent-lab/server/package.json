{
  "name": "hi-agent-lab-server",
  "version": "0.1.1",
  "type": "module",
  "private": true,
  "description": "Hi-agent Lab 的 MCP server —— 读本地 labs/ 课件，暴露给 Claude Code",
  "scripts": {
    "build": "npx --yes esbuild server.js --bundle --platform=node --format=esm --outfile=dist/server.js"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
